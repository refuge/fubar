%% -*- tab-width: 4;erlang-indent-level: 4;indent-tabs-mode: nil -*-
%% ex: ft=erlang ts=4 sw=4 et

{application, fubar,
    [
        {description, "Scalable MQTT message broker"},
        {vsn, "3.0.0"},
        {registered, []},
        {applications, [kernel, stdlib, sasl, lager, cpg, mnesia, ssl,
                        ranch, cowboy]},
        {mod, {fubar_app, []}},
        {env, []}
        ]
    }.
